syntax = "proto3";

package test.shadowing;

import "google/protobuf/any.proto";

// A user message named like the well-known type / typing name.
message Any {
  string x = 1;
}

message Note {
  string text = 1;
}

message Env {
  google.protobuf.Any payload = 1;
}
